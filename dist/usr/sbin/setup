#!/usr/bin/env bash

# Runs a command in a context, or more precise, function `$1_$2'.
# Substitutes all for the second parameter if not given.
run () {
  context=$1
  command=$2

  if [ _$command = _ ]; then
    command=all
  fi

  ${context}_$command
}

setup_all () {
  run setup assets
  run setup db
}

setup_assets () {
  run setup_assets
}

setup_assets_all () {
  run assets clobber
  run assets precompile
}

setup_assets_clean () {
  sudo -u www-data bundle exec rake assets:clean
}

setup_assets_clobber () {
  sudo -u www-data bundle exec rake assets:clobber
}

setup_assets_precompile () {
  sudo -u www-data bundle exec rake assets:precompile
}

setup_db () {
  run setup_db "$@"
}

setup_db_all () {
  run setup_db setup
}

setup_db_create () {
  sudo -u www-data bundle exec rake db:create
}

setup_db_migrate () {
  sudo -u www-data bundle exec rake db:migrate
}

setup_db_seed () {
  sudo -u www-data bundle exec rake db:seed
}

setup_db_setup () {
  sudo -u www-data bundle exec rake db:setup
}

cd /var/www/app
run setup "$@"
